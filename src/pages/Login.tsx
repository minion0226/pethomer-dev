import { 
  IonContent, 
  IonPage, 
  IonTitle, 
} from '@ionic/react';

const Login: React.FC = () => {
  return (
    <IonPage>
      <IonContent fullscreen>
        <IonTitle>Login Page</IonTitle>
      </IonContent>
    </IonPage>
  );
};

export default Login;